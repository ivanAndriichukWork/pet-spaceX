import * as auth from './rest/auth'

import {
    setToken, isBrowser,
    getUser,
    isLoggedIn,
    handleLogin,
    logout,
} from "./someVariables";

export default {
    auth
}

export { setToken, isBrowser,
    getUser,
    isLoggedIn,
    handleLogin,
    logout}
